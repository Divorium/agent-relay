---
argument_specs:
  main:
    short_description: Connect a prepared Agent Relay runner to GitHub
    options:
      agent_relay_runner_labels:
        type: list
        elements: str
        required: true
        description: Custom GitHub runner labels managed from Ansible inventory.
